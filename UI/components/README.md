# UI Components

这里记录可复用组件的设计说明。当前 GitHub Pages 版本使用原生 HTML / CSS / JavaScript 实现，不安装前端依赖。

组件清单：

- `status-panel`：版本与项目状态。
- `asset-card`：素材入口和文件预览。
- `prompt-list`：提示词列表与内容查看。
- `preview-grid`：视觉预览图展示。
- `register-table`：输出登记表。
- `queue-table`：出图队列表。
